<?php

  if (isset($_POST['submit'])) {

    // ----------------------- PROCEDURAL ------------------
    // $fileupload = $_FILES['file'];
    // $filename = $fileupload['name'];
    // $fileerror = $fileupload['error'];
    // $filesize = $fileupload['size'];
    // $filetmp_name = $fileupload['tmp_name'];

    // if ($fileerror === 0) {
    //   if ($filesize <=5000000) {
    //     $filenamechaner = explode(".",$filename);
    //     $fileextension = end($filenamechaner);
    //     $fileextarr = array('jpg', 'jpeg', 'png');
    //     if (in_array($fileextension, $fileextarr)) {
    //       $filenewName = uniqid("",true).".".$fileextension;
         
    //       $filedestination = "../uploads/".$filenewName;
    //       move_uploaded_file($filetmp_name,$filedestination);
    //       echo $filedestination;
    //       // rename("/userIMGaral/$filenewName","/uploads/.$filenewName");
    //     }
    //   }
    // }

    

  }